# Secrets
.env*
*.secret

# Backups
backups/

# Editor
*.save
*.swp
*~
.DS_Store

# Runner work directory (ARC/local runner artifact)
runner/

# K8s secrets (plain text - SealedSecret만 커밋)
**/secret.yaml
**/secret-*.yaml
sealed-secrets-key-backup.yaml

# Workspace (임시 리서치 산출물)
_workspace/

# Terraform
**/.terraform/
*.tfplan
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
