# Secrets
.env*
*.secret

# Backups
backups/

# Editor
*.save
*.swp
*~
.DS_Store

# Runner work directory (ARC/local runner artifact)
runner/

# K8s secrets (plain text - SealedSecret만 커밋)
**/secret.yaml
**/secret-*.yaml
sealed-secrets-key-backup.yaml

# Terraform
**/.terraform/
*.tfplan
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
