name: Test Create App Without Composite
on:
  workflow_call:
    inputs:
      app-name: { required: true, type: string }
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo only
        run: echo "Would create app=${{ inputs.app-name }}"
