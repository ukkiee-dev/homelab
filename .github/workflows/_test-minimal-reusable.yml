name: Test Minimal Reusable
on:
  workflow_call:
    inputs:
      app-name:
        required: true
        type: string
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from minimal reusable - app=${{ inputs.app-name }}"
