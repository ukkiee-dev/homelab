apiVersion: v1
kind: ResourceQuota
metadata:
  name: memory-quota
  namespace: actions-runner-system
spec:
  hard:
    requests.cpu: "2"
    limits.cpu: "4"
    requests.memory: 1536Mi
    limits.memory: 3Gi
