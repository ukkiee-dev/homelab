apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: networking
spec:
  limits:
    - type: Container
      default:
        memory: 64Mi
      defaultRequest:
        memory: 32Mi
      max:
        memory: 256Mi
