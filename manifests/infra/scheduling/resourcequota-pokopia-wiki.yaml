apiVersion: v1
kind: ResourceQuota
metadata:
  name: memory-quota
  namespace: pokopia-wiki
spec:
  hard:
    # Phase 6 (CNPG migration): pg Cluster (requests 256Mi / limits 512Mi) +
    # 향후 wiki 앱 pod 여유.
    requests.memory: 640Mi
    limits.memory: 1280Mi
