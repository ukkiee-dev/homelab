apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - priority-classes.yaml
  - limitrange-immich.yaml
  - limitrange-argocd.yaml
  - limitrange-monitoring.yaml
  - limitrange-apps.yaml
  - limitrange-tailscale-system.yaml
  - limitrange-traefik-system.yaml
  - limitrange-networking.yaml
  - limitrange-test-web.yaml
  # ResourceQuota는 모든 워크로드 리소스 조정 완료 후 활성화
  # helm upgrade (ArgoCD, Reloader, PostgreSQL) 완료 후 아래 주석 해제
  # - resourcequota-immich.yaml
  # - resourcequota-argocd.yaml
  # - resourcequota-monitoring.yaml
  # - resourcequota-apps.yaml
  # - resourcequota-tailscale-system.yaml
  # - resourcequota-traefik-system.yaml
  # - resourcequota-networking.yaml
  # - resourcequota-test-web.yaml
