apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: immich
spec:
  limits:
    - type: Container
      default:
        memory: 256Mi
      defaultRequest:
        memory: 64Mi
      max:
        memory: 1536Mi
