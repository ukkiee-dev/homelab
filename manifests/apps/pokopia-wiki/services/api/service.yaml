apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: pokopia-wiki
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: pokopia-wiki
spec:
  selector:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: pokopia-wiki
  ports:
    - port: 3000
      targetPort: 3000
