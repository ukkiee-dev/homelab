apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: test-app
  namespace: test-app
  labels:
    app.kubernetes.io/name: test-app
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`test-app.ukkiee.dev`)
      kind: Rule
      middlewares:
        - name: security-headers
          namespace: traefik-system
        - name: gzip
          namespace: traefik-system
      services:
        - name: test-app
          port: 3000
