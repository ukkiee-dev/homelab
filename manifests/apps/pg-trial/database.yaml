apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: demo
  namespace: pg-trial
spec:
  cluster:
    name: pg-trial-pg
  name: demo
  owner: demo
  # v0.4 리뷰 I-2a: ArgoCD prune 방어, 기본값 명시적 박제 (design §A.8)
  databaseReclaimPolicy: retain
