apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  # Phase 4 Task 4.4: on-demand Backup 트리거 (R2 업로드 검증용, 1회성)
  # 매니페스트 보관 목적으로 Git 에 포함 (실행 후에도 삭제 안 함, CR 이력 유지)
  name: pg-trial-manual-001
  namespace: pg-trial
spec:
  cluster:
    name: pg-trial-pg
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
