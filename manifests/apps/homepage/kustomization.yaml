apiVersion: kustomize.config.k8s.io/v1
kind: Kustomization

namespace: apps

resources:
  - rbac.yaml
  - configmap.yaml
  - rollout.yaml
  - service.yaml
  - ingressroute.yaml
  - sealed-secret.yaml
